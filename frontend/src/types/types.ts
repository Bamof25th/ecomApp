export interface User {
  name: string;
  email: string;
  photo: string;
  gender: string;
  role: string;
  dob: string;
  _id: string;
}
export interface Product {
  name: string;
  category: string;
  price: number;
  stock: number;
  photo: string;
  _id: string;
}
